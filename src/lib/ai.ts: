// lib/ai.ts - EMERGENCY OVERRIDE TO STOP THE ERROR
console.warn("⚠️ Using safety AI override - client-side initialization blocked");

const dummyProvider = {
  chat: {
    completions: {
      create: async () => ({ choices: [{ message: { content: "AI is not available right now." } }] })
    }
  }
};

export const createOpenAI = () => dummyProvider;
export const openai = dummyProvider;
export const createGroq = () => dummyProvider;
export const groq = dummyProvider;
export const createAnthropic = () => dummyProvider;
export const anthropic = dummyProvider;

// Default export for any wildcard imports
export default dummyProvider;
