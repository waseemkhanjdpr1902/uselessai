// lib/ai.ts - Safety file to prevent browser crash
console.warn("AI client initialized via safety file");

export const createOpenAI = () => {
  return {
    chat: { completions: { create: async () => ({}) } }
  };
};

export const openai = createOpenAI();
export const groq = createOpenAI();
export const anthropic = createOpenAI();
