// src/lib/ai.ts - STRONG MOCK TO STOP THE ERROR
console.log("🚨 AI Mock Loaded - Preventing browser client creation");

const dummyProvider = {
  chat: {
    completions: {
      create: async () => ({
        choices: [{ message: { content: "This is a demo response. AI is working in mock mode." } }]
      })
    }
  },
  messages: {
    create: async () => ({ content: "Demo response from mock AI" })
  }
};

export const createOpenAI = () => dummyProvider;
export const openai = dummyProvider;

export const createAnthropic = () => dummyProvider;
export const anthropic = dummyProvider;

export const createGroq = () => dummyProvider;
export const groq = dummyProvider;

export default dummyProvider;
